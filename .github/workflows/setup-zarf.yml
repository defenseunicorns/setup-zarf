on: [push]
jobs:
  setup_zarf:
    runs-on: macos-latest
    name: Setup Zarf
    steps:
      # To use this repository's private action,
      # you must check out the repository
      - name: Checkout
        uses: actions/checkout@v3
        with:
          ref: 'setup-zarf'
      - name: Setup Zarf
        uses: ./
        # with:
          # version: ''
      # - name: Check Zarf Version
      #   run: |
      #     zarf version