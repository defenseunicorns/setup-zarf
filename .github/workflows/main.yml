on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to install Zarf
    steps:
      # To use this repository's private action,
      # you must check out the repository
      - name: Checkout
        uses: actions/checkout@v3
      - name: Install Zarf
        uses: ./ # Uses an action in the root directory
        id: setup-zarf
        # with:
          # version: ''
        run: |
          zarf version