{
  "name": "setup-zarf",
  "version": "1.0.0",
  "description": "A GitHub Action for Installing Zarf",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build index.js -o dist --source-map --license LICENSE",
    "lint": "eslint . --fix",
    "all": "npm run lint && npm run build"
  },
  "repository": "https://github.com/defenseunicorns/setup-zarf.git",
  "author": "Defense Unicorns",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/io": "^1.1.2",
    "@actions/tool-cache": "^2.0.1",
    "fs": "^0.0.1-security",
    "os": "^0.1.2",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.34.0",
    "eslint": "^8.25.0"
  }
}
